---
paths:
  - "**/*.rs"
---

# Rust Lingua Trigger

When working on a Rust file, read `{reference-root}/standards/rust/principles.md` if it is not already in context — it elaborates the §-directives in Rust-specific form.
