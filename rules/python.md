---
paths:
  - "**/*.py"
---

# Python Lingua Trigger

When working on a Python file, read `{reference-root}/standards/python/principles.md` if it is not already in context — it elaborates the §-directives in Python-specific form.
